{% extends "regFormSection.tpl" %}

{% block section %}
    <div class="{{ classes.content }}">
        <div>
            <p class="{{ classes.text }}">{{ section.content }}</p>
        </div>
    </div>
{% endblock %}
